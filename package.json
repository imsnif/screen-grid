{
  "name": "screen-grid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "standard && tape ./test/unit/*.spec.js --tap | tap-spec",
    "coverage": "nyc --reporter lcov npm test",
    "coveralls": "nyc npm test && nyc report --reporter=text-lcov | coveralls"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron": "^1.4.2"
  },
  "devDependencies": {
    "coveralls": "^2.11.15",
    "nyc": "^9.0.1",
    "proxyquire": "^1.7.10",
    "sinon": "^1.17.6",
    "standard": "^8.5.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.2"
  }
}
